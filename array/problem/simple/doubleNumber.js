/**
*@Double Values

Use map to return a new array where each value is doubled.

Input: [1, 2, 3]

Output: [2, 4, 6]
*/

const single = [1, 2, 3];

const result = single.map((item) => item * 2);

console.log(result);
