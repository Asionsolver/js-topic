/**
*@Create Array from String

*Use Array.from() to create an array from a string.

*Input: 'hello'

Output: ['h', 'e', 'l', 'l', 'o']
*/

function arrayFrom(input) {
  return Array.from(input);
}

console.log(arrayFrom("hello"));
